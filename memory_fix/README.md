# Memory Fix Experiments

This folder holds isolated memory-selection experiments for Infinite-World.

- `last5_chunks/`: simple baseline fix that always selects the latest five 64-frame history chunks before temporal compression

Each subfolder is intended to be runnable on its own without modifying the main repo.
