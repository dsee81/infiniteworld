# Infinite World - original demo sample with replaced actions
# Format: [prompt, condition_image_path, action_json_path]
prompts:
  - - A serene campus walkway lined with modern glass buildings, green ivy climbing some walls, empty benches, soft dappled sunlight through maple trees. The motion first moves forward while the camera turns left, then moves backward while the camera turns right.
    - ./assets/example_case/0001.jpg
    - ./assets/example_case/0001_forward_backward_camera_lr.json
