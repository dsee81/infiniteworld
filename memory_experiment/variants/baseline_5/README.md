# Uniform Spacing 5

This is the baseline-style uniform temporal spacing variant.

History path:
- no inference-time preselection
- model-side 5-window evenly spaced sliding coverage
- `5 x 64 -> 80 -> 20`, then append the last frame
